# Team — shared strategy

move randomly for now

<!-- Shared directives go here. A per-ghost `strategy:` in its own file wins;
     otherwise a `strategy:` here applies to every ghost.
     Recognised by the local stub: random | chase | intercept | guard -->
